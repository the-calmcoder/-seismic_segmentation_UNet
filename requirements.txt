# =============================================================================
# Seismic Fault & Horizon Segmentation — Dependencies
# =============================================================================
# For Google Colab:  !pip install segyio
# (PyTorch, NumPy, matplotlib, scikit-learn are pre-installed on Colab)
#
# For local install:
#   pip install torch torchvision --index-url https://download.pytorch.org/whl/cu121
#   pip install -r requirements.txt
# =============================================================================

numpy>=1.24.0
segyio>=1.9.0
matplotlib>=3.7.0
scikit-learn>=1.3.0
tqdm>=4.65.0
